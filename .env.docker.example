# Copy to .env on the server. Never commit the real password.
POSTGRES_DB=flight_viewer
POSTGRES_USER=flight_viewer
POSTGRES_PASSWORD=replace-with-a-long-random-password

PUBLIC_BASE_URL=https://flight-viewer-api.moritzknodler.com
ALLOWED_ORIGINS=https://flight-viewer.moritzknodler.com
ALLOWED_ORIGIN_REGEX=^https://flight-viewer-[a-z0-9-]+\.vercel\.app$
